[package]
name = "aula-cli"
version = "0.1.0"
edition = "2021"
description = "CLI tool for interacting with the Aula school platform"

[dependencies]
aula-api = { path = "../aula-api" }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
